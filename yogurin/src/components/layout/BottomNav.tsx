export default function BottomNav() {
  return (
    <nav
      className="h-[var(--bottom-nav-height)] bg-surface flex
        justify-around items-center w-full max-w-[393px] border-t border-border fixed bottom-0 safe-bottom"
    >
      BottomNav
    </nav>
  );
}
